apiVersion: triggers.tekton.dev/v1beta1
kind: EventListener
metadata:
  name: nodejs-dev-x03-listener
spec:
  serviceAccountName: trigger-sa 
  triggers:
    - bindings:
        - ref: nodejs-dev-x03-trigger-binding
      template:
        ref: nodejs-dev-x03-pipeline-template
      interceptors:
      - ref:
          name: gitlab
          kind: ClusterInterceptor
        params:
        - name: "secretRef"
          value:
            secretName: gitlab-webhooksecret
            secretKey: webhook_secret
        - name: "eventTypes"
          value: ["Push Hook"]
