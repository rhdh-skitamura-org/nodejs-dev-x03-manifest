apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: webhook-nodejs-dev-x03-el 
spec:
  tls:
    termination: edge
  to:
    kind: Service
    name: el-nodejs-dev-x03-listener
    weight: 100
  port:
    targetPort: http-listener
  wildcardPolicy: None
